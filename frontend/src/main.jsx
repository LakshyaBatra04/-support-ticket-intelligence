import React from "react";
import ReactDOM from "react-dom/client";
import SupportIntelligenceDashboard from "./support_intelligence.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SupportIntelligenceDashboard />
  </React.StrictMode>
);